{
  "name": "abandon",
  "description": "",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "unit": "export POWERTOOLS_DEV=true && vitest run",
    "test": "npm run unit --",
    "test:coverage": "export POWERTOOLS_DEV=true && vitest run --coverage",
    "deploy": "../../deploy.sh",
    "compile": "tsc"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "4.1.0",
    "aws-sdk-client-mock-vitest": "7.0.1"
  }
}
