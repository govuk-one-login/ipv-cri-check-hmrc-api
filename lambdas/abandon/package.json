{
  "name": "abandon",
  "description": "",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "unit": "export POWERTOOLS_DEV=true && vitest run --silent=passed-only ",
    "test": "npm run unit --",
    "test:coverage": "npm run unit -- --coverage",
    "deploy": "../../deploy.sh",
    "compile": "tsc"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "4.1.0",
    "aws-sdk-client-mock-vitest": "^6.2.1"
  }
}
